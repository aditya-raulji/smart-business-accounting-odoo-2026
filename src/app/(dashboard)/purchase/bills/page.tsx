import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function VendorBillsPage() {
  return (
    <PlaceholderPage
      title="Vendor Bills"
      subtitle="Supplier invoices received, matching purchase orders, and recorded in Accounts Payable."
      moduleName="Vendor Bills"
      upcomingPrompt="Prompt 2 (Purchase Flow)"
    />
  );
}
