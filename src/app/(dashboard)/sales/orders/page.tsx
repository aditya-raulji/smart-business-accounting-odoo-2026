import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function SalesOrdersPage() {
  return (
    <PlaceholderPage
      title="Sales Orders"
      subtitle="Quotations and confirmed sales orders from retail and commercial clients."
      moduleName="Sales"
      upcomingPrompt="Prompt 3 (Sales Flow)"
    />
  );
}
