import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function CustomerReceiptsPage() {
  return (
    <PlaceholderPage
      title="Receipts & Collections"
      subtitle="Inward customer payments matched against outstanding invoices."
      moduleName="Receipts"
      upcomingPrompt="Prompt 3 (Sales Flow)"
    />
  );
}
